# Aurora Gateway — no external services required (SQLite is embedded)
# This file is intentionally empty after Redis removal.
# SQLite database (aurora.db) is created automatically by the application.
